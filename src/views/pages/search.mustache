{{>partials/header}}

{{#banner}}
    <section class="banner" style="background-image: url('images/{{bg}}')"></section>
    <h2>Buscando por: {{title}}</h2>
{{/banner}}

<div class="container list">
    {{#pets}}
        <div class="item">
            <img src="images/{{image}}" class="item--image" />
            <div class="item--name">{{name}}</div>
            <div class="item--color">Cor: {{color}}</div>
            <div class="item--genre">Gênero: {{sex}}</div>
        </div>
    {{/pets}}
</div>

{{>partials/footer}}